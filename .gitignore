# Python
__pycache__/
*.py[cod]
*.log

# The Bonsai 2 model files, server logs and cache live in this same folder.
# Keep the repo lean: only the assistant app + server scripts are versioned, not the model.
.cache/
*.gguf
# tool output (screenshots, scraped text, OCR results)
out/

# Legacy Needle model package leftovers (kept for reference, not versioned)
checkpoints/
python/
tokenizer/
assets/
*.cact
config.json
README.needle.md
.gitattributes
android-*/
ios-*
linux-*/
macos-*/
tvos-*/
watchos-*/
wasm/
wasm-component/
windows-*/

# Piper TTS voice models + audio - fetch them with piper/download_voices.py
piper/*.onnx
piper/*.onnx.json
piper/*.wav

# Model selector config (per-machine model list + active choice)
models.json

# Scheduled shell tasks (per-machine state written by schedule_task)
schedules.json
schedules.json.tmp

# OS / editor
Thumbs.db
Desktop.ini
.vscode/
.idea/